services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: docs-dashboard
    restart: unless-stopped
    networks:
      - internal-net

networks:
  internal-net:
    external: true
